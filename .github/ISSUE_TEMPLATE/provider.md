---
name: New provider
about: Track scraping coverage for one street-view provider
title: "[T?] <Provider name> (<key>)"
labels: provider
---

<!--
Provider issues are normally created by the `add-provider` skill, which uses the
approved subplan `docs/providers/<key>.md` as the issue body
(`gh issue create --body-file ...`).

If you are filing this by hand, paste the filled subplan here — see the template
at docs/templates/provider_subplan.md. A provider must not start implementation
until its subplan is approved by a maintainer (docs/PLAN.md §5).
-->

## Subplan

_Paste the contents of `docs/providers/<key>.md` here._
