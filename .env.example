# Copy to .env and fill in. .env is gitignored — never commit real tokens.
# Each provider that needs auth documents its key name in docs/providers/<key>.md.

# Mapillary client/access token (already-working provider; vector tiles need it)
MAPILLARY_ACCESS_TOKEN=

# Example slots for upcoming providers that require auth (filled by scout specs):
# BARIKOI_API_KEY=
# MAPPLS_CLIENT_ID=
# MAPPLS_CLIENT_SECRET=
