// Copy this file to config.local.js, then replace the placeholder manually.
// config.local.js is git-ignored. Restrict this static key by domain/IP in the
// Mappls Console; for the default notebook configuration allow 127.0.0.1.
window.MAPPLS_CONFIG = {
  accessToken: "PASTE_YOUR_MAPPLS_STATIC_KEY_HERE",
};
